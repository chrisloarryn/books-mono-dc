# API Environment
PORT=3001
MONGO_URI=mongodb://localhost:27017/bookreviews
# Optional basic auth (if both set, API requires it)
BASIC_AUTH_USER=
BASIC_AUTH_PASS=
# Public base URL of the API (used to build absolute URLs for covers)
API_BASE_URL=http://localhost:3001
