# Nuxt public runtime configuration
# Base URL for the API (must include /api suffix)
NUXT_PUBLIC_API_BASE=http://localhost:3001/api
# Optional Basic Auth credentials (should match API if enabled)
NUXT_PUBLIC_BASIC_AUTH_USER=
NUXT_PUBLIC_BASIC_AUTH_PASS=
